---
import { LandingHero } from '@/partials/LandingHero';
import { ServicesGrid } from '@/partials/ServicesGrid';
import { GeneralCTA } from '@/partials/GeneralCTA';
import Basefi from '@/templates/Basefi.astro';
import { AppConfig } from '@/utils/AppConfig';

const title = 'Davitt Barry - IT-ammattilainen | Muusikko | Ääniteknikko';
const { description } = AppConfig;
---

<Basefi head={{ title, description }}>
  <LandingHero locale="fi" />
  <ServicesGrid locale="fi" />
  <GeneralCTA locale="fi" />
</Basefi>
